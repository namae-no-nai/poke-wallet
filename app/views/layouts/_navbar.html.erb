<nav class="navbar navbar-dark navbar-expand-lg nav-color">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">
      <img src="/pokeball.png" alt="logo" style="width: 48px;" />
    </a>
    <button class="navbar-toggler toggler-color" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/logs">Purchase History</a>
        </li>
        <% if investor_signed_in? %>
          <li class="nav-item">
            <%= link_to('Sign Out', destroy_investor_session_path, data: { turbo_method: :delete } , class: 'nav-link') %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to('Sign In', new_investor_session_path, class: 'nav-link') %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>